import SwiftUI

extension Color {
    /// VocabLook brand accent — ink-indigo, matches the approved mockups.
    static let inkIndigo = Color(red: 91.0 / 255.0, green: 91.0 / 255.0, blue: 214.0 / 255.0)
}
